# p20 — Rewrite README to match current project state

## 1. Work summary

The `README.md` was stale — it described the p01/p02 app (Home/Stats/Settings,
"3 sample habits", no auth) and a data model with no `User`/`Tag` models and no
`userId`/`targetDays` fields. Rewrote it end-to-end to reflect the app as it
stands after p01–p19, verifying every claim against the current source.

Sections now in the README:

- **Features** — accounts/auth & per-user data isolation, habit CRUD, daily
  check-ins, target days, streaks & the weekly completion chart, tags/search/
  filter, monthly calendar, light/dark theme, and the accessibility/production
  polish (loading skeletons, error boundaries, custom 404, per-page metadata).
- **Tech stack** — Next.js 16 (App Router) + React 19, TypeScript strict,
  Tailwind v4, Prisma 6/SQLite, Auth.js (NextAuth v5) + bcryptjs, visx,
  next-themes, zod, Vitest + Playwright.
- **Setup / run** — `npm install` → `db:migrate` → `db:seed` → `dev`, plus the
  seeded demo accounts (`alice@test.com` / `bob@test.com`, `password123`) and an
  accurate `.env` note (now covers `DATABASE_URL` **and** the dev-only
  `AUTH_SECRET`, replacing the old "contains no secrets" line).
- **Testing** — how to run the Vitest unit suite and the Playwright E2E suite
  (including `npx playwright install chromium` and the throwaway `prisma/test.db`
  on port 3100), plus build/tsc/lint/format commands.
- **npm scripts** — full table, now including `test` and `test:e2e`.
- **Project structure** — current tree (app routes incl. auth/error/loading/
  not-found, `components/ui` + `components/features`, `lib`, `actions`, `prisma`,
  `tests/unit`, `tests/e2e`, `types`, `proxy.ts`, `bench-reports`).
- **Data model** — updated to the four current models (User, Habit with
  `userId`/`targetDays`, CheckIn, Tag) with their constraints.
- **Architecture notes** — Server Components by default, mutations via Server
  Actions, defense-in-depth auth (`proxy.ts` + data-layer checks).

Facts were cross-checked against source rather than assumed: `package.json`
scripts, `DATABASE_URL="file:./dev.db"`, the E2E port (3100) and `test.db` /
`migrate deploy` setup, the seed output (2 users, 4 habits, 5 tags, ~2 weeks of
check-ins), and the Prisma schema. The rewritten file was formatted with Prettier
so it passes `format:check`.

## 2. Files changed

- `README.md` — full rewrite (only file modified).
- `bench-reports/p20.md` — this report.

No source code changed.

## 3. Verification

- `npx prettier --check README.md` — clean (matches project code style).
- `npm run lint` — 0 errors.
- `npx tsc --noEmit` — 0 errors.
- `git status` — only `README.md` modified (no stray files).

> Note: repo-wide `npm run format:check` still flags a few **pre-existing**
> files (`AUDIT.md`, `prisma/seed.ts`, and two others) that were already
> non-conformant before this task. They are unrelated to the README and were left
> untouched to keep this change scoped. `build`/E2E were not re-run because no
> runtime code changed.

## 4. Timing

- **Start:** 2026-07-02T16:57:04Z
- **End:** 2026-07-02T17:00:21Z
- **Elapsed:** ~3m 17s

TOKENS: TBD
